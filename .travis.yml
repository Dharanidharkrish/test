language: java
jdk:
    -openjdk8
    
deploy:
    - provider: s3
      access_key_id: $AWS_ID
      secret_access_key: $AWS_KEY
      bucket: 'buckets132'
      ##region: 'us-east-2'
      skip_cleanup: true
